[package]
name = "dpdk-sys"
version = "0.1.0"
edition = "2021"
description = "Raw FFI bindings to DPDK"

[dependencies]
libc = { workspace = true }

[build-dependencies]
bindgen = "0.70"

[lib]
name = "dpdk_sys"
