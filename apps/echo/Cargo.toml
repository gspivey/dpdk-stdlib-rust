[package]
name = "echo"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.0", features = ["derive"] }
dpdk-udp = { path = "../../dpdk-udp", optional = true }

[features]
default = []
dpdk = ["dep:dpdk-udp"]
