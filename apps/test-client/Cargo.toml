[package]
name = "test-client"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.0", features = ["derive"] }
tokio = { version = "1.0", features = ["net", "rt-multi-thread", "macros", "time"] }
